<Window x:Class="micdah.LrControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:micdah.LrControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="300">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Width="100">
                <StackPanel Orientation="Horizontal">
                    <TextBlock>Connected: </TextBlock>
                    <TextBlock x:Name="Connected">?</TextBlock>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>API Version: </TextBlock>
                    <TextBlock x:Name="ApiVersion">?</TextBlock>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Padding="3" Margin="3" Click="GetAllParameterValues_OnClick">Do stuff</Button>
            <TextBox Grid.Row="1" x:Name="Response" IsReadOnly="True"/>

            <Button Grid.Row="2" Padding="3" Margin="3" Click="Decrement_OnClick">Decrement exposure</Button>
        </Grid>
    </DockPanel>
</Window>
